project: ${PROJECT_NAME}
runs_dir: ${RUNS_DIR}
data: configs/dataset.yaml
model: yolov8n.pt
epochs: 100
imgsz: 640
batch: 16
workers: 8
patience: 20
device: ${DEVICE}
save: true
save_period: -1
exist_ok: false
verbose: true
plots: true
optimizer: auto
lr0: 0.01
lrf: 0.01
momentum: 0.937
weight_decay: 0.0005
warmup_epochs: 3.0
warmup_momentum: 0.8
warmup_bias_lr: 0.1
augment: true
val: true
seed: 42
rect: false
cache: false
